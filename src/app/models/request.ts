export class Request {
   constructor(public id: string, public dates: string, public days: string, public note: string, public reason: string, public status: string, public submit_time: string, public type: string, public user_id: string) {}
}
